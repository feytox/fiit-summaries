### Определение: Линейный функционал
```spoiler-markdown
Пусть $V$ - векторное пространство над $F$.
**Линейный функционал** на $V$ - это линейный оператор $g: V \to F$
```

### Строение линейного функционала
Формулировка:
```spoiler-markdown
Пусть $V$ - конечномерное пространство со скалярным произведение над $F \in {\mathbb{R}, \mathbb{C}}$, а $\Phi: V \to F$ - линейный функционал. Тогда существует единственный вектор $a \in V$ такой, что $\forall{x \in V}~~ \Phi(x) = xa$
```

Д-во:
```spoiler-markdown
**Единственность.**
Из [[Аксиомы евклидовых и унитарных пространства.#Ослабленный закон сокращения|ослабленного закона сокращения]] следует, что:
$$a, b \in V \land \forall{x \in V}~~  \Phi(x) = xa = xb \Rightarrow a = b$$

**Существование.**
Если $\forall{x \in V}~~ \Phi(x) = 0$, то в качестве $a$ можно выбрать $a = 0$. Поэтому будем считать, что $\Phi$ принимает не только значение 0.

По [[Ядро и образ линейного оператора. Теорема о сумме ранга и дефекта. Алгоритм одновременного вычисления ядра и образа#Теорема о сумме ранга и дефекта|теореме о сумме ранга и дефекта]]: $\mathrm{Ker}~\Phi$ - подпространство размерности $\mathrm{dim}~V - 1$ (так как $r(\Phi) = 1$), а его ортодоп $(\mathrm{Ker}~\Phi)^{\perp}$ - одномерное подпространство в $V$. 
Пусть $0 \neq b \in (\mathrm{Ker}~\Phi)^{\perp}$, $\beta := \Phi(b), a := \dfrac{\overline{\beta}}{b^{2}}b$. Проверим, что $\forall{x \in V}~~  \Phi(x) = xa$. Для этого представим $x$ в виде $x = c + \gamma b$ для $c \in \mathrm{Ker}~\Phi$ и $\gamma \in F$. Такое представление возможно, так как $V = \mathrm{Ker}~\Phi \oplus (\mathrm{Ker}~\Phi)^{\perp}$, а одномерное подпространство $\mathrm{Ker}~(\Phi)^{\perp}$ порождается вектором $b$. Тогда:
$$\Phi(x) = \Phi(c + \gamma b) = \Phi(c) + \Phi(\gamma b) = \gamma\Phi(b) = \gamma \beta$$
так как $\Phi(c) = 0$. С другой стороны:
$$xa = (c + \gamma b)\dfrac{\overline{\beta}}{b^{2}}b = c\dfrac{\overline{\beta}}{b^{2}}b + \gamma b \dfrac{\overline{\beta}}{b^{2}}b = \gamma\beta$$
так как $cb = 0$. Значит $\forall{x \in V}~~ \Phi(x) = xa$ $~~~\square$
```

### * Замечание
```spoiler-markdown
В бесконечномерных пространствах со скалярным произведением некоторые линейные функционалы представимы в виде скалярного произведения с подходящим вектором, а некоторые - нет.
```