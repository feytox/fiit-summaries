Определение:
```spoiler-markdown
$K$ называется **компактным**, если из любого открытого покрытия можно выделить конечное подпокрытие, то есть:
$$\forall{\{G_{k}\}}~~ \exists{\{G_{\beta}\}_{\beta=1}^{n}}\mathpunct{:}~~ K \subset \bigcup_{\beta=1}^{n} G_{\beta}$$
```
