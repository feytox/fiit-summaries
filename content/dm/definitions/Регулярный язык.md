## Регулярный язык
Определение:
```spoiler-markdown
Язык называется **регулярным**, если он получается из элементарных языков (т.е. $\varnothing$, $\{\lambda\}$, $\{a_{i}\}$, $a_{i} \in \Sigma$) с помощью объединения, умножения и итерации.
```
