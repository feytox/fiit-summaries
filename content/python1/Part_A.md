# ФАЙЛ В РАЗРАБОТКЕ

### Задание 1
Какие из перечисленных методов есть у класса `str`, но отсутствуют у `bytes`?
- ☐ `count`
- ☐ `encode`
- ☐ `casefold`
- ☐ `decode`
- ☐ `fromhex`
- ☐ `isdecimal`
- ☐ `index`
Ответ:
`casefold`
`isdecimal`
`encode`

### Задание 2
Каких режимов открытия файла не существует?
- ☐ `b`
- ☐ `rw`
- ☐ `wb`
- ☐ `r+`
- ☐ `x`
Ответ:
`b`
`rw`

### Задание 3
Объекты каких классов может вернуть метод `read` у файла?
- ☐ `bytes`
- ☐ `list`
- ☐ `int`
- ☐ `str`
Ответ:
`bytes`
`str`

### Задание 4
Выберите верные утверждения. Остаток от деления...
- ☐ ... всегда положителен
- ☐ ... может быть больше делителя
- ☐ ... может быть больше делимого
- ☐ ... всегда неположителен если делитель отрицатеelen
Ответ:
может быть больше делителя
может быть больше делимого
всегда неположителен если делитель отрицатеelen

### Задание 5
Каким образом задаётся заглядывание назад?
- ○ `(?<...)`
- ○ другое
- ○ `(?!...)`
- ○ никак
- ○ `(?:...)`
Ответ:
другое

### Задание 6
Какие из перечисленных объектов можно положить в `set`?
- ☐ `'1'`
- ☐ `r'1'`
- ☐ `frozenset(1)`
- ☐ `(1, 1)`
- ☐ `[1]`
- ☐ `bytearray()`
Ответ:
`'1'`
`r'1'`
`(1, 1)`

### Задание 7
Выберите верное утверждение для приведённого кода:
```python
class A:
    def f(self):
        return 1
    def f(self, x):
        return x
print(A().f())
```
- ○ код напечатает 1
- ○ класс `A` содержит синтаксическую ошибку, будет выброшено `SyntaxError`
- ○ при вызове `A.f` без параметров будет выброшено `TypeError`
Ответ:
при вызове `A.f` без параметров будет выброшено `TypeError`

### Задание 8
Дан словарь `d` и ключ `k`. Какая разница между выражениями `d[k]` и `d.get(k)`?
- ○ разницы нет
- ○ разница есть, но другая
- ○ если ключ `k` отсутствует, первое вернёт `None`, а второе выбросит исключение
- ○ если ключ `k` отсутствует, второе выбросит исключение
- ○ если ключ `k` отсутствует, первое выбросит исключение, а второе вернёт `None`
Ответ: 
если ключ `k` отсутствует, первое выбросит исключение, а второе вернёт `None`

### Задание 9
Какой тип возвращаемого значения у `re.findall`?
- ○ кортеж
- ○ итератор
- ○ список
- ○ генератор
Ответ:
список

### Задание 10
Функция объявлена так, как показано ниже. Какие способы вызова функции корректны?
```python
def f(x, *y, **z):
    pass
```
- ☐ `f(**{})`
- ☐ `f(10)`
- ☐ `f()`
- ☐ `f(**{10})`
- ☐ `f(*[10])`
- ☐ `f(10, a=10)`
Ответ:
`f(10)`
`f(*[10])`
`f(10, a=10)`

### Задание 11
Объектом какого класса является функция?
- ○ никаким
- ○ `function`
- ○ `Function`
- ○ `code`
- ○ ничего из перечисленного
Ответ:
`function`

### Задание 12
Какой метод позволяет преобразовать объект типа `str` в объект типа `bytearray`?
- ○ `encode`
- ○ такого метода нет
- ○ `decode`
- ○ другое
Ответ:
такого метода нет

### Задание 13
Укажите все выражения, результатом которых является `1`.
```python
class A:
    def f():
        return 1
```
- ☐ `A.f()`
- ☐ `A.f(A())`
- ☐ `A().f()`
- ☐ `A::f()`
- ☐ `A.f`
Ответ:
`A.f()`

### Задание 14
Выберите верные утверждения. Остаток от деления...
- ☐ ... всегда положителен
- ☐ ... может быть больше делителя
- ☐ ... может быть больше делимого
- ☐ ... всегда неположителен если делитель отрицателен
Ответ:
может быть больше делителя
может быть больше делимого
всегда неположителен если делитель отрицателен

### Задание 15
Каким образом задаётся заглядывание вперёд?
- ○ никак
- ○ `(?=...)`
- ○ `(?!...)`
- ○ другое
- ○ `(?>...)`

### Задание 16
Каких режимов открытия файла не существует?
- ☐ `rw`
- ☐ `ab`
- ☐ `r+b`
- ☐ `rb`
- ☐ `wb`

### Задание 17
Как узнать имя функции `f`?
- ○ `f.__name__`
- ○ другое
- ○ никак
- ○ `f.name`
Ответ:
`f.__name__`

### Задание 18
Как создать одноэлементный `tuple`?
- ○ `(1)`
- ○ `(1,)`
- ○ `tuple(1)`
Ответ:
`(1,)`

### Задание 19
Дан файловый объект `f`. Как получить текущую позицию в файле?
- ○ `f.pos`
- ○ `f.seek()`
- ○ другое
- ○ `f.tell()`
- ○ никак

### Задание 20
Как внутри метода `m` приведённого класса обратиться к полю `x` объекта?
```python
class A:
    def m(obj):
        ...
```
- ○ `self.x`
- ○ `obj.x`
- ○ другое
- ○ `this.x`
- ○ `x`
- ○ никак

### Задание 21
Какие из этих приведённых операций поддерживаются в Python?
- ☐ //
- ☐ *
- ☐ `%%`
- ☐ ++
- ☐ /
- ☐ %
- ☐ +
- ☐ **

### Задание 22
Объекты каких классов может вернуть метод `read` у файла?
- ☐ `bytes`
- ☐ `list`
- ☐ `int`
- ☐ `str`

### Задание 23
Каким образом задаётся заглядывание назад?
- ○ `(?<...)`
- ○ другое
- ○ `(?!...)`
- ○ никак
- ○ `(?:...)`

### Задание 24
Какой метод позволяет преобразовать объект типа `str` в объект типа `bytes`?
- ○ другое
- ○ `decode`
- ○ такого метода нет
- ○ `encode`

### Задание 25
Выберите верное утверждение для приведённого кода:
```python
class A:
    def f(self):
        return 1
    def f(self, x):
        return x
print(A().f())
```
- ○ код напечатает 1
- ○ класс `A` содержит синтаксическую ошибку, будет выброшено `SyntaxError`
- ○ при вызове `A.f` без параметров будет выброшено `TypeError`

### Задание 26
Что из перечисленного является `tuple`?
- ☐ `(1)`
- ☐ `1,`
- ☐ `1, 2`
- ☐ `(1, 2)`

### Задание 27
Дан класс `A`. Как вызвать метод `__meth`?
```python
class A:
    def __meth(self):
        return 1
```
- ○ `A._A__meth()`
- ○ другое
- ○ `A().__meth()`
- ○ никак
- ○ `A()._A__meth()`
- ○ `A.__meth()`

### Задание 28
Какая функция модуля `re` матчит соответствие только строки целиком?
- ○ `re.fullmatch`
- ○ `re.match`
- ○ `re.search`

### Задание 29
Функция объявлена так, как показано ниже. Какие способы вызова функции корректны?
```python
def f(*x):
    pass
```
- ☐ `f(*[1, 2, 3])`
- ☐ `f(x=1)`
- ☐ `f((1, 2))`
- ☐ `f({'x': 5})`
- ☐ `f(a=2, b=3)`
- ☐ `f({'a': 5})`

### Задание 30
Дан файловый объект `f`. Как получить текущую позицию в файле?
- ○ `f.pos`
- ○ `f.seek()`
- ○ другое
- ○ `f.tell()`
- ○ никак

### Задание 31
Что произойдёт, если в `set` добавить элемент, который уже содержится в нём?
- ○ вылетит исключение
- ○ элемент не добавится, метод добавления вернёт `False`
- ○ элемент не добавится, метод добавления вернёт `None`

### Задание 32
Каких режимов открытия файла не существует?
- ☐ `b`
- ☐ `rw`
- ☐ `wb`
- ☐ `r+`
- ☐ `x`

### Задание 33
Укажите все выражения, результатом которых является `1`.
```python
class A:
    @classmethod
    def f(cls):
        return 1
```
- ☐ `A.f()`
- ☐ `A.f(A())`
- ☐ `A.f`
- ☐ `A().f()`
- ☐ `A::f()`

### Задание 34
Что возвращает метод `__new__` класса?
- ○ созданный объект класса
- ○ другое
- ○ ничего

### Задание 35
Какие из перечисленных объектов можно положить в `set`?
- ☐ `None`
- ☐ `1j`
- ☐ `True`
- ☐ `()`
- ☐ `f'1'`
- ☐ `{1: 1}`

### Задание 36
Является ли синтаксически корректным выражение `3 > 2 > 1`?
- ○ да
- ○ нет

### Задание 37
Что делает метод `replace` класса `str`?
- ○ другое
- ○ заменяет в строке вхождения подстроки на заданную
- ○ создаёт новую строку, в которой заменены вхождения подстроки на заданную и возвращает её
- ○ такого метода нет

### Задание 38
Какая функция модуля `re` матчит соответствие где-то в строке?
- ○ `re.fullmatch`
- ○ `re.search`
- ○ `re.match`

### Задание 39
Дан файловый объект `f`. Как получить текущую позицию в файле?
- ○ `f.pos`
- ○ `f.seek()`
- ○ другое
- ○ `f.tell()`
- ○ никак

### Задание 40
Функция объявлена так, как показано ниже. Какие способы вызова функции корректны?
```python
def f(x, *y, **z):
    pass
```
- ☐ `f(**{})`
- ☐ `f(10)`
- ☐ `f()`
- ☐ `f(**{10})`
- ☐ `f(*[10])`
- ☐ `f(10, a=10)`

### Задание 41
Какие из перечисленных объектов можно положить в `set`?
- ☐ `None`
- ☐ `1j`
- ☐ `True`
- ☐ `()`
- ☐ `f'1'`
- ☐ `{1: 1}`

### Задание 42
Объекты каких классов может вернуть метод `read` у файла?
- ☐ `bytes`
- ☐ `list`
- ☐ `int`
- ☐ `str`

### Задание 43
Как проверить, что класс `B` является наследником класса `A`?
- ○ никак
- ○ другое
- ○ `issubclass(B, A)`
- ○ `B in A.__bases__`
- ○ `issubclass(A, B)`

### Задание 44
Укажите все выражения, результатом которых является `1`.
```python
class A:
    def f(self):
        return 1
```
- ☐ `A.f(A())`
- ☐ `A::f()`
- ☐ `A.f()`
- ☐ `A.f`
- ☐ `A().f()`

### Задание 45
Является ли синтаксически корректным выражение `3 > 2 > 1`?
- ○ да
- ○ нет

### Задание 46
Каким образом задаётся заглядывание вперёд?
- ○ никак
- ○ `(?=...)`
- ○ `(?!...)`
- ○ другое
- ○ `(?>...)`

### Задание 47
Что выведет код?
```python
def f(xs=[]):
    xs.append(1)
    return xs
f()
print(f())
```
- ○ другое
- ○ ничего
- ○ `[1]`
- ○ `[1, 1]`

### Задание 48
Каких режимов открытия файла не существует?
- ☐ `rw`
- ☐ `ab`
- ☐ `r+b`
- ☐ `rb`
- ☐ `wb`

### Задание 49
Что из перечисленного является встроенным типом в Python?
- ☐ `str`
- ☐ `bytes`
- ☐ `unicode`
- ☐ `char`
- ☐ `bytearray`

### Задание 50
Какие из перечисленных объектов можно положить в `set`?
- ☐ `'1'`
- ☐ `r'1'`
- ☐ `frozenset(1)`
- ☐ `(1, 1)`
- ☐ `[1]`
- ☐ `bytearray()`

### Задание 51
Укажите все выражения, результатом которых является `1`.
```python
class A:
    @classmethod
    def f(cls):
        return 1
```
- ☐ `A.f()`
- ☐ `A.f(A())`
- ☐ `A.f`
- ☐ `A().f()`
- ☐ `A::f()`

### Задание 52
Как узнать докстринг функции `f`?
- ○ никак
- ○ другое
- ○ `f.__docstring__`
- ○ `f.__doc__`

### Задание 53
Является ли синтаксически корректным выражение `1 < 2 < 3`?
- ○ да
- ○ нет

### Задание 54
Объекты каких классов может вернуть метод `read` у файла?
- ☐ `bytes`
- ☐ `list`
- ☐ `int`
- ☐ `str`

### Задание 55
Можно ли класс объявить внутри класса / внутри функции?
- ○ да / да
- ○ да / нет
- ○ нет / нет
- ○ нет / да

### Задание 56
Как узнать имя функции `f`?
- ○ `f.__name__`
- ○ другое
- ○ никак
- ○ `f.name`

### Задание 57
Дан файловый объект `f`. Как получить текущую позицию в файле?
- ○ `f.pos`
- ○ `f.seek()`
- ○ другое
- ○ `f.tell()`
- ○ никак

### Задание 58
Какие фрагменты кода создадут словари?
- ☐ `dict(['one', 'two', 'three'])`
- ☐ `{'one':1, 'two':2, 'three':3}`
- ☐ `{'one', 'two', 'three'}`
- ☐ `dict([('one':1), ('two':2), ('three':3)])`
- ☐ `dict([('one', 1), ('two', 2), ('three', 3)])`

### Задание 59
Какая функция модуля `re` матчит соответствие только в начале строки?
- ○ `re.fullmatch`
- ○ `re.search`
- ○ `re.match`

### Задание 60
Какие фрагменты кода создадут множество с одним элементом?
- ☐ `set('aaa')`
- ☐ `set({1})`
- ☐ `{{1}}`
- ☐ `set('aaAAaaaAa')`
- ☐ `{1, 1}`

### Задание 61
Функция объявлена так, как показано ниже. Какие способы вызова функции корректны?
```python
def f(x, *y, **z):
    pass
```
- ☐ `f(**{})`
- ☐ `f(10)`
- ☐ `f()`
- ☐ `f(**{10})`
- ☐ `f(*[10])`
- ☐ `f(10, a=10)`

### Задание 62
Как внутри метода `m` приведённого класса обратиться к полю `x` объекта?
```python
class A:
    def m(obj):
        ...
```
- ○ `self.x`
- ○ `obj.x`
- ○ другое
- ○ `this.x`
- ○ `x`
- ○ никак

### Задание 63
Дан словарь `d` и ключ `k`. Какая разница между выражениями `d[k]` и `d.get(k)`?
- ○ разницы нет
- ○ разница есть, но другая
- ○ если ключ `k` отсутствует, первое вернёт `None`, а второе выбросит исключение
- ○ если ключ `k` отсутствует, второе выбросит исключение
- ○ если ключ `k` отсутствует, первое выбросит исключение, а второе вернёт `None`

### Задание 64
Объекты каких типов являются изменяемыми?
- ☐ `tuple`
- ☐ `set`
- ☐ `frozenset`
- ☐ `dict`
- ☐ `list`

### Задание 65
Каких режимов открытия файла не существует?
- ☐ `rw`
- ☐ `ab`
- ☐ `r+b`
- ☐ `rb`
- ☐ `wb`

### Задание 66
Чему эквивалентна запись `a+` в регулярных выражениях?
- ☐ `a{1,}`
- ☐ `a{0,}`
- ☐ `a{1}`
- ☐ `a{1,1}`
- ☐ `a{0,1}`

### Задание 67
Укажите все выражения, результатом которых является `1`.
```python
class A:
    def f(self):
        return 1
```
- ☐ `A.f(A())`
- ☐ `A::f()`
- ☐ `A.f()`
- ☐ `A.f`
- ☐ `A().f()`

### Задание 68
Чему эквивалентна запись `a**b**c`?
- ○ `a**(b**c)`
- ○ `(a**b)**c`

### Задание 69
Можно ли класс объявить внутри класса / внутри функции?
- ○ да / да
- ○ да / нет
- ○ нет / нет
- ○ нет / да

### Задание 70
Какие из указанных определений функции корректны?
- ☐ `def f(x):`
- ☐ `def f(x, y=1):`
- ☐ `def f(x, **k, y):`
- ☐ `def f(x=1, y):`
- ☐ `def f(x, *t):`
- ☐ `def f():`
- ☐ `def f(x, **k):`

### Задание 71
Какие из указанных определений функции корректны?
- ☐ `def f(x=1, y):`
- ☐ `def f(**k, *t):`
- ☐ `def f(x, *t):`
- ☐ `def f(x, y=1, **k):`
- ☐ `def f(x, y=1):`
- ☐ `def f(x, **k):`
- ☐ `def f(x, **k, y):`

### Задание 72
Какие из этих приведённых операций поддерживаются в Python?
- ☐ //
- ☐ *
- ☐ %%
- ☐ ++
- ☐ /
- ☐ %
- ☐ +
- ☐ **

### Задание 73
Укажите все выражения, результатом которых является `1`.
```python
class A:
    def f():
        return 1
```
- ☐ `A.f()`
- ☐ `A.f(A())`
- ☐ `A().f()`
- ☐ `A::f()`
- ☐ `A.f`

### Задание 74
Чему эквивалентна запись `a*` в регулярных выражениях?
- ☐ `a{1,}`
- ☐ `a{0,1}`
- ☐ `a{1,1}`
- ☐ `a{0,}`

### Задание 75
Является ли синтаксически корректным выражение `3 > 2 > 1`?
- ○ да
- ○ нет

### Задание 76
Какие из перечисленных объектов можно положить в `set`?
- ☐ `{1}`
- ☐ `1/1`
- ☐ `b''`
- ☐ `(1)`
- ☐ `([1], 1)`
- ☐ `'1'`

### Задание 77
Объектом какого класса является функция?
- ○ никаким
- ○ `function`
- ○ `Function`
- ○ `code`
- ○ ничего из перечисленного

### Задание 78
Выберите верные утверждения. Остаток от деления...
- ☐ ... всегда положителен
- ☐ ... может быть больше делителя
- ☐ ... может быть больше делимого
- ☐ ... всегда неположителен если делитель отрицателен

### Задание 79
Чему эквивалентна запись `a*` в регулярных выражениях?
- ☐ `a{1,}`
- ☐ `a{0,1}`
- ☐ `a{1,1}`
- ☐ `a{0,}`

### Задание 80
Как проверить, что класс `B` является наследником класса `A`?
- ○ никак
- ○ другое
- ○ `issubclass(B, A)`
- ○ `B in A.__bases__`
- ○ `issubclass(A, B)`

### Задание 81
Каким образом задаётся заглядывание вперёд?
- ○ никак
- ○ `(?=...)`
- ○ `(?!...)`
- ○ другое

### Задание 82
Каких режимов открытия файла не существует?
- ☐ `rw`
- ☐ `ab`
- ☐ `r+b`
- ☐ `rb`
- ☐ `wb`

### Задание 83
Дана строка `s`. Как узнать её длину?
- ○ `s.size`
- ○ `s.__len__`
- ○ `len(s)`
- ○ `length(s)`
- ○ `s.length`

### Задание 84
Чему эквивалентна запись `a**b**c`?
- ○ `a**(b**c)`
- ○ `(a**b)**c`

### Задание 85
Есть ли различия между объявлениями классов `A`, `B` и `C`?
```python
class A:
    pass
class B():
    pass
class C(object):
    pass
```
- ○ нет отличий
- ○ отличается `B`
- ○ все различны
- ○ отличается `A`
- ○ отличается `C`

### Задание 86
Что делает метод `replace` класса `str`?
- ○ другое
- ○ заменяет в строке вхождения подстроки на заданную
- ○ создаёт новую строку, в которой заменены вхождения подстроки на заданную и возвращает её
- ○ такого метода нет

### Задание 87
Какие из перечисленных объектов можно положить в `set`?
- ☐ `'1'`
- ☐ `r'1'`
- ☐ `frozenset(1)`
- ☐ `(1, 1)`
- ☐ `[1]`
- ☐ `bytearray()`

### Задание 88
Выберите верное утверждение для приведённого кода:
```python
class A:
    def f(self):
        return 1
    def f(self, x):
        return x
print(A().f())
```
- ○ код напечатает 1
- ○ класс `A` содержит синтаксическую ошибку, будет выброшено `SyntaxError`
- ○ при вызове `A.f` без параметров будет выброшено `TypeError`

### Задание 89
Что из перечисленного является `tuple`?
- ☐ `(1)`
- ☐ `1,`
- ☐ `1, 2`
- ☐ `(1, 2)`

### Задание 90
Какая функция модуля `re` матчит соответствие только строки целиком?
- ○ `re.fullmatch`
- ○ `re.match`
- ○ `re.search`

### Задание 91
Функция объявлена так, как показано ниже. Какие способы вызова функции корректны?
```python
def f(*x):
    pass
```
- ☐ `f(*[1, 2, 3])`
- ☐ `f(x=1)`
- ☐ `f((1, 2))`
- ☐ `f({'x': 5})`
- ☐ `f(a=2, b=3)`
- ☐ `f({'a': 5})`
